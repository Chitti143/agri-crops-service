spring:
  application:
    name: agri-crops-service

  datasource:
    url: jdbc:mysql://localhost:3306/agri_farmers
    username: root
    password: Chitti@149
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8093

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
